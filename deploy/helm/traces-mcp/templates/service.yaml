apiVersion: v1
kind: Service
metadata:
  name: {{ include "traces-mcp.fullname" . }}
  labels:
    {{- include "traces-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traces-mcp.selectorLabels" . | nindent 4 }}